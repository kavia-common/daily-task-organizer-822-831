package org.example.app

/**
 * AndroidTest-visible alias class to reuse the JVM fake without depending on Android framework.
 * This is placed in androidTest to ensure it's available to instrumented tests' source set.
 */
typealias LocalDataStoreFakeForTests = LocalDataStoreTestFacade
